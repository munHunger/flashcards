name: flashcards
ports:
  - 5003
monitoring:
  port: 4001
scripts:
  start: "cd server; npm run start monitoring=4001 server=5003"
  build: "cd client; npm install && npm run build; cd ../server; npm install; cd .."
  test:
